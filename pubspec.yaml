name: archive_manager_v3
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  provider: ^6.1.2
  file_picker: ^10.3.3
  flutter_colorpicker: ^1.1.0
  window_manager: ^0.5.1
  path_provider: ^2.1.5
  uuid: ^4.3.3
  vector_math: ^2.1.4
  path: ^1.9.1
  crypto: ^3.0.6
  image: ^4.1.7
  super_drag_and_drop: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
